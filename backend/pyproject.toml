[project]
name = "mindease-backend"
version = "0.1.0"
description = "MindEase mental wellness app backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.0",
]
